import { HttpHeaders } from "./http";
import { ILogger } from "./Logger";
export declare class StreamChooser {
    private logger;
    private streamUrl;
    private httpHeaders?;
    private manifest?;
    constructor(logger: ILogger, streamUrl: string, httpHeaders?: HttpHeaders | undefined);
    load(): Promise<boolean>;
    isMaster(): boolean;
    getPlaylistUrl(maxBandwidth?: "worst" | "best" | number): string | false;
}
