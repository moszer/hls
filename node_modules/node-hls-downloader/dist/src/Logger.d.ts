import { IConfig } from "./Config";
export interface ILogger {
    log: (...params: any) => void;
    error: (...params: any) => void;
}
export declare function buildLogger(logger: IConfig["logger"]): ILogger;
