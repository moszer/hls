import { ChunksDownloader } from "./ChunksDownloader";
import { HttpHeaders } from "./http";
import { ILogger } from "./Logger";
export declare class ChunksLiveDownloader extends ChunksDownloader {
    private fromEnd;
    private timeoutDuration;
    private playlistRefreshInterval;
    private lastSegment?;
    private timeoutHandle?;
    private refreshHandle?;
    constructor(logger: ILogger, playlistUrl: string, concurrency: number, fromEnd: number, segmentDirectory: string, timeoutDuration?: number, playlistRefreshInterval?: number, httpHeaders?: HttpHeaders);
    protected refreshPlayList(): Promise<void>;
    private timeout;
}
